{
  "name": "root",
  "private": true,
  "devDependencies": {
    "cross-port-killer": "1.4.0",
    "lerna": "4.0.0",
    "minimist": "1.2.5",
    "rimraf": "3.0.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "node ./scripts/prepare-common-ui.js && npx kill-port 3333 &&  lerna run start --stream",
    "start:fast": "node ./scripts/prepare-common-ui.js --fastMode && npx kill-port 3333 &&  lerna run start --stream",
    "build:fe": "node ./scripts/prepare-common-ui.js &&  lerna run build --stream --ignore package-backend",
    "lint": "lerna run lint --stream",
    "lint:fix": "lerna run lint:fix --stream",
    "deploy:fe": "npm run build:fe && lerna run deploy --stream --ignore package-backend"
  }
}
