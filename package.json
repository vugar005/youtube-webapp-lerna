{
  "name": "root",
  "private": true,
  "devDependencies": {
    "cross-port-killer": "1.4.0",
    "lerna": "4.0.0",
    "minimist": "1.2.5",
    "rimraf": "3.0.2"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "node ./scripts/prepare-common-ui.js && npx kill-port 3333 &&  lerna run start --stream",
    "start:fast": "node ./scripts/prepare-common-ui.js --fastMode && npx kill-port 3333 &&  lerna run start --stream",
    "build": "node ./scripts/prepare-common-ui.js &&  lerna run build --stream"
  }
}
